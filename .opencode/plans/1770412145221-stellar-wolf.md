Goal: add root/workspace scripts for format/lint/typecheck + repo lint + scan.

Files to edit:
- /root/projects/chronops/package.json
- /root/projects/chronops/packages/web/package.json
- /root/projects/chronops/packages/functions/package.json
- /root/projects/chronops/packages/domain/package.json
- /root/projects/chronops/packages/core/package.json

Plan:
1) Root scripts update in `package.json`.
   - Replace fmt/fmt:check with format/format:fix.
   - Ensure root scripts use `bun --filter "*" <cmd>` for format/lint/typecheck.
   - Add `lint:repo` = `sherif`, `lint:repo:fix` = `sherif --fix`.
   - Add `scan` = run `lint` + `format` + `typecheck` + `lint:repo`.
   - Add `scan:fix` = run `lint:fix` + `format:fix` + `typecheck` + `lint:repo:fix`.
2) Workspace package scripts add/confirm in each `packages/*/package.json`.
   - `typecheck`: `tsc --noEmit` (already exists, keep).
   - Add `lint`: `oxlint`.
   - Add `lint:fix`: `oxlint --fix`.
   - Add `format`: `oxfmt`.
   - Add `format:fix`: `oxfmt --fix`.
3) Verify by checking scripts set in each package and root.

Verification:
- `bun run typecheck`
- `bun run lint`
- `bun run format`
- `bun run lint:repo`
- `bun run scan`

Unresolved questions: none.
